name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    # iTerm2 は macOS 専用 = 実デプロイ先。ubuntu は動かさない OS なので CI 対象にしない
    # （非ターゲットの挙動差で赤くなるのを避ける）。
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      # ユニット＋契約テストは iTerm2 不要（FakeAdapter）。結合テストは
      # ITERM2_CLI_INTEGRATION 未設定なので skip される。
      - name: Lint (ruff)
        run: uv run --python ${{ matrix.python-version }} ruff check src tests

      - name: Test (pytest)
        run: uv run --python ${{ matrix.python-version }} pytest -q
